<?xml version="1.0" encoding="UTF-8"?>
<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
	<f:entry title="${%yml.title}" field="yml" description="${%yml.description}">
		<f:textbox />
	</f:entry>
	<f:entry title="${%schema.title}" field="schema" description="${%schema.description}">
		<f:textbox />
	</f:entry>
</j:jelly>